# .github

Default community health files and organisation-wide configuration for [@ewanc26](https://github.com/ewanc26)'s repositories.

## What's here

- `FUNDING.yml` — sponsor/funding links surfaced on all repos
- `CONTRIBUTING.md` — default contributing guidelines
- `SECURITY.md` — vulnerability reporting policy
- `profile/README.md` — GitHub profile README (local reference only; must live in the `ewanc26` repo to render)
- `gitignore/` — reference `.gitignore` templates by stack
  - `sveltekit.gitignore`
  - `python.gitignore`
  - `node-ts.gitignore`
  - `swift-xcode.gitignore`
  - `nes-6502.gitignore`
- `gitattributes/` — reference `.gitattributes` templates
  - `default.gitattributes` — `* text=auto` (universal)

> `.gitignore` and `.gitattributes` templates are **not** inherited by repos — copy them in manually when starting a new project.

## Sponsorship

If you find my work useful, consider supporting me via [GitHub Sponsors](https://github.com/sponsors/ewanc26) or [Ko-fi](https://ko-fi.com/ewancroft).
